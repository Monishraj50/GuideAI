{
  "name": "guideai",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "description": "Multi-agent company platform — you're the boss, agents are your team.",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green --kill-others-on-fail \"tsx watch apps/server/src/index.ts\" \"cd apps/web && ./node_modules/.bin/next dev -p 3000\"",
    "build": "echo 'build runs per-package later'",
    "init-db": "tsx packages/shared/src/db/init.ts",
    "lint": "echo 'lint added in later step'",
    "typecheck": "tsc --noEmit -p apps/server/tsconfig.json && tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/policies/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "concurrently": "^10.0.3",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
