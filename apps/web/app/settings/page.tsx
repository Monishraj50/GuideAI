export default function SettingsPage() {
  return (
    <div className="p-6 text-dim text-sm">
      <div className="text-ink font-medium mb-2">Settings</div>
      <p>Auto-approval rules + model tiers + MCP allowlist + budgets land in step 8.</p>
    </div>
  );
}
